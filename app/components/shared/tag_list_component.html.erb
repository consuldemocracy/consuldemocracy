<% if machine_learning? && taggable.tag_list_on(:ml_tags).present? %>
  <%= render MachineLearning::InfoComponent.new %>
<% end %>

<%= link_list(*links, class: "tags", id: "tags_#{dom_id(taggable)}") %>
