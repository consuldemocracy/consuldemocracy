<div class="detailed-info">
  <%= render "/shared/author_info", resource: record %>

  <span class="bullet">&nbsp;&bull;&nbsp;</span>
  <%= l record.created_at.to_date %>
  <span class="bullet">&nbsp;&bull;&nbsp;</span>
  <%= render Shared::CommentsCountComponent.new(record.comments_count, url: comments_path) %>

  <%= content %>

  <% if can?(:flag, record) && !preview? %>
    <span class="bullet">&nbsp;&bull;&nbsp;</span>
    <span class="js-flag-actions">
      <%= render "shared/flag_actions", flaggable: record %>
    </span>
  <% end %>
</div>
