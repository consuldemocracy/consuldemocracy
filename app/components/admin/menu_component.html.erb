<%= link_list(
  (proposals_link if feature?(:proposals)),
  (debates_link if feature?(:debates)),
  comments_link,
  (polls_link if feature?(:polls)),
  (legislation_link if feature?(:legislation)),
  (budgets_link if feature?(:budgets)),
  item_title("booths") + booths_list,
  (signature_sheets_link if feature?(:signature_sheets)),
  item_title("messages") + messages_list,
  item_title("site_customization") + site_customization_list,
  item_title("moderated_content") + moderated_content_list,
  item_title("profiles") + profiles_list,
  stats_link,
  item_title("settings") + settings_list,
  item_title("dashboard") + dashboard_list,
  id: "admin_menu", data: { "accordion-menu": true, "multi-open": true }
) %>
