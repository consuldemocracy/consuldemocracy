<%= form_for poll, url: answer_poll_path(poll), method: :post, class: "poll-form" do |f| %>
  <% questions.each do |question| %>
    <%= render Polls::Questions::QuestionComponent.new(question) %>
  <% end %>

  <%= f.submit(class: "button", value: t("polls.form.vote")) %>
<% end %>
