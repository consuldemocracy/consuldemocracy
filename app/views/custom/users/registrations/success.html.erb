<% provide(:title) { t("devise_views.users.registrations.success.title") } %>

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-sm-8 col-md-6 col-lg-4 justify-content-center">

        <div class="h2 fw-bold text-center"><%= t("devise_views.users.registrations.success.title") %></div>

        <%= sanitize(t("devise_views.users.registrations.success.thank_you")) %>
        <%= sanitize(t("devise_views.users.registrations.success.instructions_1")) %>
        <%= t("devise_views.users.registrations.success.instructions_2") %>

        <%= link_to t("devise_views.users.registrations.success.back_to_index"), root_path, class: "btn btn-primary w-100" %>
      </div>
    </div>
  </div>
