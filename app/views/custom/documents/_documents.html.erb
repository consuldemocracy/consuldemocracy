<% if documents.any? %>
  <div id="documents" class="documents">
    <h4><%= t("documents.title") %>&nbsp;<span>(<%= documents.count %>)</span></h4>

    <ul class="no-bullet document-link">
      <%= render partial: "documents/document", collection: documents %>
    </ul>
  </div>
<% end %>
