<h3 class="title"><%= t("welcome.cards.title") %></h3>

<div class="row">
  <div class="cards-container">
    <% @cards.each do |card| %>
      <%= render "card", card: card %>
    <% end %>
  </div>
</div>
