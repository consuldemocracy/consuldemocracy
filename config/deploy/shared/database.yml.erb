<%= fetch(:rails_env) %>:
  adapter: postgresql
  timeout: 5000
  encoding: unicode
  reconnect: false
  database: <%= "#{fetch(:application)}" %>
  pool: 5
  username:
  password:
  host: <%= fetch(:db_server) %>
